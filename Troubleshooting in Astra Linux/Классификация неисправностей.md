### Классификация неисправностей, их диагностика и устранение в Astra Linux
#### 1. Классификация неисправностей, вызвавших нештатную ситуацию
Нештатная ситуация — это любое отклонение работы системы от штатного режима, приводящее к снижению доступности, производительности или нарушению целостности данных. Для эффективной диагностики важно правильно классифицировать причину сбоя.

Основные категории неисправностей:

Категория           | Источник                  | Примеры
------------------- | ------------------------- | ----------------------------
Аппаратные	 | Физические компоненты	  | Отказ диска, перегрев CPU, сбой ОЗУ, неисправность сетевой карты, проблемы с БП
Программные  | (системные)	ОС, драйверы, приложения	  | Ошибки ядра, повреждение файловой системы, конфликты библиотек, утечки памяти
Сетевые	 | Сетевые интерфейсы, протоколы	  | Обрыв канала, потери пакетов, ошибки DNS, конфликт IP, отказ маршрутизации
Вызванные действиями пользователей	 | Ошибки администратора или пользователя	  | Неверная правка конфигурации, случайное удаление файлов, неверные права доступа

В подсистеме безопасности	PARSEC, мандатный доступ	Ошибка меток безопасности, блокировка мандатным контролем, сбои аудита

Признаки, указывающие на категорию:
- Аппаратные: внезапные перезагрузки, ошибки в dmesg, I/O errors, шум/перегрев.
- Программные: зависания процессов, ошибки в логах приложений, kernel panic, повреждение конфигов.
- Сетевые: недоступность узлов, высокий ping, потери пакетов, ошибки резолвинга.
- Пользовательские: «вчера работало», изменения перед сбоем, ошибки прав.
- Безопасности: отказ в доступе с сообщениями PARSEC, ошибки pdp-*, блокировки аудита.

#### 2. Документирование и фиксация изменений в системе
Без фиксации изменений диагностика превращается в угадывание. Основные принципы:
- Ведите журнал изменений: дата, время, автор, что изменено, зачем.
- Перед изменениями: сохраняйте резервные копии конфигов (cp file file.bak.$(date +%F)).
- Используйте системы контроля версий для критичных конфигов (etckeeper, git).
- Фиксируйте состояние до и после изменений (pcs status, ip a, ss -tulnp).
- Документируйте инциденты: симптом, гипотеза, действие, результат.

Пример фиксации:

```text
Дата: 2026-09-20 14:30
Автор: admin
Изменение: правка /etc/network/interfaces — смена IP eth0
Причина: миграция в новую подсеть
Результат: сеть работает, проверено ping/traceroute
Откат: cp /etc/network/interfaces.bak.2026-09-20 /etc/network/interfaces
```
#### 3. Сбор информации об аппаратных и программных компонентах
Аппаратные компоненты:

Инструмент          | Назначение
------------------- | -------------------------
lscpu	              | Информация о процессоре
lsblk, fdisk -l	    | Список дисков и разделов
lspci, lsusb	      | Устройства PCI и USB
dmidecode	          | Информация BIOS/SMBIOS (RAM, материнская плата)
smartctl	          | Состояние SMART жёстких дисков
sensors	            | Температура, вентиляторы, напряжение
free -h	            | Использование ОЗУ
dmesg	              | Сообщения ядра о железе

Программные компоненты:

Инструмент          | Назначение
------------------- | -------------------------
uname -a	 | Версия ядра
cat /etc/astra_version	 | Версия Astra Linux
dpkg -l	 | Установленные пакеты
systemctl list-units --failed	 | Упавшие службы
apt list --upgradable	 | Доступные обновления

Пример сбора общей картины:

```bash
# Собрать сводку в один файл
{
  echo "=== Система ==="; uname -a; cat /etc/astra_version
  echo "=== CPU ==="; lscpu | head -20
  echo "=== RAM ==="; free -h
  echo "=== Диски ==="; lsblk; df -h
  echo "=== Сеть ==="; ip a; ip r
  echo "=== Службы (failed) ==="; systemctl --failed
  echo "=== Последние ошибки ==="; journalctl -p err -b | tail -50
} > /tmp/diag_$(date +%F_%H%M).txt
```
#### 4. Методология поиска и решения неисправностей
Пошаговый алгоритм:
- Сбор информации. Что именно не работает? Когда началось? Что менялось? Кто заметил?
- Локализация. Определить уровень: железо → ОС → сеть → приложение → пользователь.
- Воспроизведение. Попытаться повторить проблему в контролируемых условиях.
- Формулирование гипотезы. На основе симптомов и логов.
- Проверка гипотезы. Минимальное изменение → наблюдение результата.
- Устранение. Применить исправление, зафиксировать.
- Проверка. Убедиться, что проблема решена и не появилось побочных эффектов.
- Документирование. Записать причину, решение, меры предотвращения.

Принципы диагностики:
- От простого к сложному: сначала проверьте кабель и питание, потом логи.
- Одно изменение за раз: не меняйте несколько параметров одновременно.
- Ищите первопричину, а не симптом: перезапуск службы лечит симптом, а не причину.
- Правило «что изменилось?»: 90% сбоев связаны с недавними изменениями.
- Изоляция: отключите подозрительные компоненты, чтобы локализовать проблему.

#### 5. Анализ системной информации в терминале и журнальных файлах
Основные источники журналов:

Источник            | Что содержит
------------------- | -------------------------
journalctl	 | Единый журнал systemd (все службы)
/var/log/syslog	 | Системные сообщения
/var/log/auth.log	  | Аутентификация, sudo, SSH
/var/log/kern.log	  | Сообщения ядра
/var/log/dpkg.log	  | Установка/удаление пакетов
/var/log/apt/history.log	  | История apt
/var/log/audit/audit.log	  | Аудит безопасности (PARSEC)
/var/log/parsec/	  | Логи подсистемы безопасности

*Полезные команды journalctl:*

```bash
# Ошибки текущей загрузки
journalctl -p err -b

# Логи конкретной службы
journalctl -u apache2 -f

# За период
journalctl --since "2026-09-20 10:00" --until "2026-09-20 12:00"

# По конкретному процессу
journalctl _PID=1234

# Ядро
journalctl -k
```
*Анализ в терминале:*

```bash
# Поиск ошибок
grep -i error /var/log/syslog | tail -50

# Отслеживание в реальном времени
tail -f /var/log/syslog /var/log/auth.log

# Подсчёт ошибок
journalctl -p err -b | wc -l
```
#### 6. Основные инструменты диагностики
##### 6.1. Процессы и нагрузка
*ps — снимок процессов:*

```bash
ps aux                    # все процессы
ps -ef --forest           # дерево процессов
ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem | head   # топ по памяти
```
*top / htop — интерактивный мониторинг:*

```bash
top -b -n 1 | head -20    # пакетный режим
# В top: P — сортировка по CPU, M — по памяти, k — kill процесса
```
##### 6.2. Память
*free — использование ОЗУ:*

```bash
free -h                   # в человеком читаемом виде
free -m                   # в мегабайтах
```
Интерпретация: столбец available — реально доступная память, а не free.

##### 6.3. Дисковая подсистема
df — использование файловых систем:

```bash
df -h                     # в человекочитаемом виде
df -i                     # inodes
df -hT                    # с типом ФС
```
*iostat — статистика ввода-вывода:*

```bash
iostat -x 2 5             # расширенная статистика каждые 2 с, 5 раз
```
Ключевые показатели: %util (загрузка диска), await (время ожидания), r/s, w/s.

*vmstat — виртуальная память и CPU:*

```bash
vmstat 2 5                # каждые 2 с, 5 раз
```
Ключевые столбцы: r (очередь CPU), si/so (swap in/out), wa (ожидание I/O).

*sar — историческая статистика (пакет sysstat):*

```bash
sar -u 1 5                # CPU
sar -r 1 5                # память
sar -d 1 5                # диски
sar -n DEV 1 5            # сетевые интерфейсы
```
##### 6.4. Сетевые инструменты
ping — проверка доступности:

```bash
ping -c 4 8.8.8.8
ping -I eth0 192.168.1.1  # через конкретный интерфейс
```
*traceroute — маршрут до узла:*

```bash
traceroute 8.8.8.8
traceroute -n 8.8.8.8     # без резолвинга имён
```
*dig — диагностика DNS:*

```bash
dig ya.ru
dig @8.8.8.8 ya.ru A
dig -x 8.8.8.8            # обратный запрос
dig ya.ru MX
```
nslookup — простой DNS-запрос:

```bash
nslookup ya.ru
nslookup ya.ru 8.8.8.8
```
*ss — сокеты (замена netstat):*

```bash
ss -tulnp                 # слушающие порты с процессами
ss -tan                   # все TCP-соединения
ss -s                     # сводка
ss -tnp state established # установленные соединения
```
*netstat — устаревший, но иногда используемый:*

```bash
netstat -tulnp
netstat -rn               # таблица маршрутизации
```
*ip — управление сетью (замена ifconfig/route):*

```bash
ip a                      # адреса
ip r                      # маршруты
ip n                      # ARP-таблица
ip -s link                # статистика интерфейсов
ip link set eth0 up
```
*nmcli — управление NetworkManager:*

```bash
nmcli device status
nmcli connection show
nmcli connection show "Wired connection 1"
nmcli device show eth0
```
*nmap — сканирование сети:*

```bash
nmap 192.168.1.0/24       # обнаружение хостов
nmap -sT 192.168.1.1      # TCP-сканирование
nmap -p 22,80,443 192.168.1.1
nmap -sV 192.168.1.1      # определение версий служб
```
*tcpdump — захват пакетов:*

```bash
tcpdump -i eth0 -n
tcpdump -i eth0 port 80 -w /tmp/http.pcap
tcpdump -i any host 192.168.1.1
tcpdump -i eth0 -nn 'tcp[tcpflags] & tcp-syn != 0'  # только SYN
```
#### 7. Примеры диагностики по категориям
##### 7.1. Аппаратная неисправность
Симптом: система периодически перезагружается.

```bash
# Проверка температуры и питания
sensors
# Проверка ошибок ядра
dmesg | grep -i -E "error|fail|mce|thermal"
# SMART дисков
sudo smartctl -a /dev/sda | grep -i -E "health|error"
# Проверка ОЗУ
sudo memtester 1G 1
```
##### 7.2. Программная неисправность
Симптом: служба не запускается.

```bash
systemctl status apache2
journalctl -u apache2 -n 50
apache2ctl configtest
systemctl list-units --failed
```
##### 7.3. Сетевая неисправность
Симптом: нет доступа к внешнему сайту.

```bash
# 1. Проверка интерфейса
ip a; ip r
# 2. Проверка шлюза
ping -c 2 192.168.1.1
# 3. Проверка DNS
dig @8.8.8.8 ya.ru
# 4. Проверка внешнего адреса
ping -c 2 8.8.8.8
# 5. Трассировка
traceroute 8.8.8.8
# 6. Захват трафика
sudo tcpdump -i eth0 -n host 8.8.8.8
```
##### 7.4. Неисправность, вызванная действиями пользователя
Симптом: после правки конфига не работает SSH.

```bash
# Проверка синтаксиса
sshd -t
# Просмотр последних изменений
ls -la /etc/ssh/sshd_config*
diff /etc/ssh/sshd_config.bak /etc/ssh/sshd_config
# Проверка логов
journalctl -u ssh -n 50
# Проверка порта
ss -tlnp | grep ssh
```
##### 7.5. Неисправность в подсистеме безопасности
Симптом: пользователь не может получить доступ к данным.

```bash
# Проверка метки пользователя
pdpl-user -i username
# Проверка метки файла
pdp-ls -M /path/to/file
# Проверка аудита
sudo tail -f /var/log/audit/audit.log
# Проверка PARSEC
journalctl -u parsec -n 50
```
#### 8. Сводная таблица инструментов по задаче

Задача              | Инструменты
------------------- | -------------------------
Процессы и CPU	 | ps, top, htop, vmstat, sar -u
Память	 | free, vmstat, sar -r
Диски и I/O	 | df, lsblk, iostat, sar -d, smartctl
Сеть — базовая	 | ip, nmcli, ping, traceroute
Сеть — DNS	 | dig, nslookup
Сеть — сокеты	 | ss, netstat
Сеть — сканирование	 | nmap
Сеть — захват трафика	 | tcpdump
Логи	journalctl, /var/log/*
Аппаратура	lscpu, lspci, dmidecode, sensors
Безопасность	pdpl-*, pdp-ls, audit.log

Примеры использования инструментов диагностики в Astra Linux
#### 9. Инструменты анализа процессов
ps — снимок процессов
```bash
# Все процессы в полном формате
ps aux

# Дерево процессов
ps -ef --forest

# Топ-10 по памяти
ps -eo pid,ppid,user,cmd,%mem,%cpu --sort=-%mem | head -11

# Топ-10 по CPU
ps -eo pid,ppid,user,cmd,%cpu,%mem --sort=-%cpu | head -11

# Процессы конкретного пользователя
ps -u postgres

# Процессы с конкретным именем
ps -C nginx -o pid,ppid,cmd,%cpu,%mem

# Процессы, запущенные после определённого времени
ps -eo pid,lstart,cmd | grep "$(date +%Y-%m-%d)"

# Процессы без терминала (демоны)
ps -ef | awk '$8 == "?" {print}'

# Найти процесс по порту (косвенно)
ps aux | grep sshd
```
Пример вывода:

```text
USER       PID %CPU %MEM    VSZ   RSS TTY   STAT START   TIME COMMAND
root         1  0.0  0.1 168556 13140 ?     Ss   10:15   0:03 /sbin/init
postgres  1234  0.5  2.3 350120 95000 ?     Ss   10:20   0:15 postgres: checkpointer
```
top — интерактивный мониторинг
```bash
# Интерактивный запуск
top

# Пакетный режим — 1 итерация, топ-20
top -b -n 1 | head -20

# Только процессы пользователя postgres
top -u postgres

# С обновлением каждые 2 секунды, 5 итераций
top -d 2 -n 5 -b > /tmp/top_report.txt
```
Горячие клавиши в top:

Клавиша             | 	Действие
------------------- | -------------------------
P	 | Сортировка по CPU
M	 | Сортировка по памяти
T	 | Сортировка по времени работы
k	 | Убить процесс (запросит PID)
r	 | Renice (изменить приоритет)
1	 | Показать все ядра CPU
c	 | Показать полную командную строку
q	 | Выход

htop — улучшенный top
```bash
sudo apt install htop
htop

# Показать только процессы конкретного пользователя
htop -u postgres

# Показать дерево процессов
htop -t
```
#### 10. Инструменты анализа памяти
free — использование ОЗУ
```bash
# Человекочитаемый формат
free -h

# В мегабайтах
free -m

# Обновление каждые 2 секунды
free -h -s 2

# Общее количество
free -h -t
```
Пример вывода:

```text
              total        used        free      shared  buff/cache   available
Mem:           15Gi       4.2Gi       1.1Gi       350Mi        10Gi        10Gi
Swap:         2.0Gi          0B       2.0Gi
```
Интерпретация:
- used — реально занятая память
- buff/cache — кэш (может быть освобождён при необходимости)
- available — реально доступная память (главный показатель)
- vmstat — виртуальная память и CPU

```bash
# Каждые 2 секунды, 5 итераций
vmstat 2 5

# С расширенной статистикой
vmstat -s

# Статистика по дискам
vmstat -d

# Статистика по слэбам ядра
vmstat -m
```
Пример вывода:

```text
procs -----------memory---------- ---swap-- -----io---- -system-- ------cpu-----
 r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st
 2  0      0 1150000 250000 10500000  0    0     5    20  150  300  5  2 92  1  0
```
Ключевые столбцы:
- r — очередь на CPU (если > числа ядер — перегрузка)
- b — процессы в ожидании I/O
- si/so — swap in/out (ненулевые — мало ОЗУ)
- wa — ожидание I/O (высокое — проблема с диском)
- sar — историческая статистика

```bash
# Установить sysstat
sudo apt install sysstat
sudo systemctl enable --now sysstat

# CPU — каждую секунду, 5 раз
sar -u 1 5

# Память
sar -r 1 5

# Swap
sar -S 1 5

# Диски
sar -d 1 5

# Сеть
sar -n DEV 1 5

# Ошибки сети
sar -n EDEV 1 5

# Исторические данные за сегодня
sar -u
sar -r
```
Пример отчёта по CPU:

```text
12:00:01    CPU     %user     %nice   %system   %iowait    %steal     %idle
12:00:02    all      5.23      0.00      2.15      1.02      0.00     91.60
```
#### 11. Инструменты анализа дисков
df — использование файловых систем
```bash
# Человекочитаемый формат
df -h

# С типом ФС
df -hT

# Inodes
df -i

# Конкретная ФС
df -h /var

# С указанием точки монтирования
df -h --output=source,size,used,avail,pcent,target
```
Пример вывода:

```text
Filesystem     Type      Size  Used Avail Use% Mounted on
/dev/sda1      ext4      100G   45G   50G  48% /
/dev/sdb1      ext4      500G  200G  275G  43% /data
tmpfs          tmpfs     7.8G     0  7.8G   0% /dev/shm
```
lsblk — список блочных устройств
```bash
# Дерево устройств
lsblk

# С информацией о ФС
lsblk -f

# С размерами в байтах
lsblk -b

# Только диски (без разделов)
lsblk -d

# С топологией
lsblk -t
```
Пример вывода:

```text
NAME   MAJ:MIN RM  SIZE RO TYPE MOUNTPOINT
sda      8:0    0  100G  0 disk
├─sda1   8:1    0  512M  0 part /boot
└─sda2   8:2    0 99.5G  0 part /
sdb      8:16   0  500G  0 disk
└─sdb1   8:17   0  500G  0 part /data
```
iostat — статистика I/O
```bash
# Установить sysstat
sudo apt install sysstat

# Базовая статистика
iostat

# Расширенная, каждые 2 с, 5 раз
iostat -x 2 5

# В мегабайтах
iostat -m

# Только диски (без CPU)
iostat -d 2 5
```
Пример вывода (ключевые столбцы):

```text
Device   r/s     w/s    rkB/s    wkB/s  await  %util
sda      5.20   10.50   250.0    500.0   1.20   2.50
sdb     50.30  120.00  2500.0   6000.0  15.40  85.20
```
Интерпретация:

- %util — загрузка диска (>80% — узкое место)
- await — среднее время ожидания I/O (мс)
- r/s, w/s — операции чтения/записи в секунду

smartctl — состояние дисков
```bash
# Установить
sudo apt install smartmontools

# Проверка здоровья
sudo smartctl -H /dev/sda

# Полная информация
sudo smartctl -a /dev/sda

# Запустить короткий тест
sudo smartctl -t short /dev/sda

# Результаты теста
sudo smartctl -l selftest /dev/sda

# Температура
sudo smartctl -A /dev/sda | grep -i temperature
```
Пример вывода:

```text
SMART overall-health self-assessment test result: PASSED
```
#### 12. Инструменты анализа сети
ip — управление сетью
```bash
# Все интерфейсы
ip a

# Конкретный интерфейс
ip a show eth0

# Только IPv4
ip -4 a

# Только IPv6
ip -6 a

# Маршруты
ip r

# Маршрут по умолчанию
ip r show default

# ARP-таблица
ip n

# Статистика интерфейса
ip -s link show eth0

# Включить/выключить интерфейс
sudo ip link set eth0 down
sudo ip link set eth0 up

# Добавить адрес
sudo ip addr add 192.168.1.100/24 dev eth0

# Добавить маршрут
sudo ip route add 10.0.0.0/8 via 192.168.1.1

# Показать namespace
ip netns list
```
nmcli — управление NetworkManager
```bash
# Статус всех устройств
nmcli device status

# Список подключений
nmcli connection show

# Детали подключения
nmcli connection show "Wired connection 1"

# Информация об устройстве
nmcli device show eth0

# Создать подключение
sudo nmcli con add type ethernet con-name eth0-static ifname eth0 \
    ip4 192.168.1.100/24 gw4 192.168.1.1

# Изменить DNS
sudo nmcli con mod eth0-static ipv4.dns "8.8.8.8 8.8.4.4"

# Активировать/деактивировать
sudo nmcli con up eth0-static
sudo nmcli con down eth0-static

# Сканирование Wi-Fi
nmcli device wifi list
```
ping — проверка доступности
```bash
# Простой ping
ping ya.ru

# Ограниченное число пакетов
ping -c 4 8.8.8.8

# Через конкретный интерфейс
ping -I eth0 8.8.8.8

# С указанием размера пакета
ping -s 1500 -c 4 8.8.8.8

# Быстрый режим (0.2 с между пакетами)
ping -i 0.2 -c 10 8.8.8.8

# Только статистика
ping -c 10 -q 8.8.8.8

# IPv6
ping6 2001:db8::1
```
traceroute — маршрут
```bash
# Базовый
traceroute 8.8.8.8

# Без резолвинга имён
traceroute -n 8.8.8.8

# С 3 запросами на хоп
traceroute -q 3 8.8.8.8

# Максимум 15 хопов
traceroute -m 15 8.8.8.8

# По TCP (если ICMP блокируется)
sudo traceroute -T -p 443 ya.ru

# IPv6
traceroute6 2001:4860:4860::8888
```
dig — DNS-диагностика
```bash
# A-запись
dig ya.ru

# Короткий ответ
dig +short ya.ru

# Запрос через конкретный сервер
dig @8.8.8.8 ya.ru

# MX-запись
dig ya.ru MX

# NS-запись
dig ya.ru NS

# TXT-запись
dig ya.ru TXT

# Обратный запрос
dig -x 8.8.8.8

# Трассировка DNS
dig +trace ya.ru

# Только ответ
dig +noall +answer ya.ru

# Проверка DNSSEC
dig +dnssec ya.ru
nslookup — простой DNS-запрос
bash
# Базовый
nslookup ya.ru

# Через конкретный сервер
nslookup ya.ru 8.8.8.8

# Обратный запрос
nslookup 8.8.8.8

# Интерактивный режим
nslookup
> server 8.8.8.8
> ya.ru
> exit
```
ss — сокеты
```bash
# Слушающие TCP-порты с процессами
ss -tlnp

# Слушающие UDP-порты
ss -ulnp

# Все соединения
ss -tan

# Установленные соединения
ss -tn state established

# Сводка
ss -s

# По порту
ss -tnp '( sport = :22 or dport = :22 )'

# По процессу
ss -tnp | grep nginx

# Статистика TCP
ss -ti
```
Пример вывода:

```text
State    Recv-Q   Send-Q   Local Address:Port    Peer Address:Port   Process
LISTEN   0        128      0.0.0.0:22            0.0.0.0:*           users:(("sshd",pid=1234,fd=3))
LISTEN   0        511      0.0.0.0:80            0.0.0.0:*           users:(("nginx",pid=2345,fd=6))
```
netstat — сокеты (устаревший)
```bash
# Слушающие порты
netstat -tulnp

# Все соединения
netstat -tan

# Маршруты
netstat -rn

# Статистика интерфейсов
netstat -i

# Статистика протоколов
netstat -s

# По PID
netstat -tunp | grep 1234
nmap — сканирование сети
bash
# Установить
sudo apt install nmap

# Обнаружение хостов в подсети
nmap -sn 192.168.1.0/24

# Быстрое сканирование топ-100 портов
nmap 192.168.1.1

# TCP SYN-сканирование (нужен root)
sudo nmap -sS 192.168.1.1

# TCP connect-сканирование
nmap -sT 192.168.1.1

# UDP-сканирование
sudo nmap -sU 192.168.1.1

# Определение версий служб
nmap -sV 192.168.1.1

# Определение ОС
sudo nmap -O 192.168.1.1

# Конкретные порты
nmap -p 22,80,443,3306 192.168.1.1

# Диапазон портов
nmap -p 1-1000 192.168.1.1

# Все порты
nmap -p- 192.168.1.1

# С использованием скриптов NSE
nmap --script http-title 192.168.1.1
nmap --script vuln 192.168.1.1

# Сохранить результат
nmap -oN scan.txt 192.168.1.0/24
nmap -oX scan.xml 192.168.1.0/24
```
tcpdump — захват пакетов
```bash
# Установить
sudo apt install tcpdump

# Базовый захват
sudo tcpdump -i eth0

# Без резолвинга имён
sudo tcpdump -i eth0 -n

# По порту
sudo tcpdump -i eth0 port 80
sudo tcpdump -i eth0 port 22

# По хосту
sudo tcpdump -i eth0 host 192.168.1.1

# По источнику/назначению
sudo tcpdump -i eth0 src 192.168.1.100
sudo tcpdump -i eth0 dst 8.8.8.8

# По протоколу
sudo tcpdump -i eth0 icmp
sudo tcpdump -i eth0 tcp
sudo tcpdump -i eth0 udp

# Комбинированный фильтр
sudo tcpdump -i eth0 'tcp port 80 and host 192.168.1.1'

# Только SYN-пакеты
sudo tcpdump -i eth0 'tcp[tcpflags] & tcp-syn != 0'

# Захват в файл
sudo tcpdump -i eth0 -w /tmp/capture.pcap

# Чтение из файла
tcpdump -r /tmp/capture.pcap -n

# Ограничить количество пакетов
sudo tcpdump -i eth0 -c 100

# Показать содержимое пакета в hex/ASCII
sudo tcpdump -i eth0 -X port 80

# С дампами пакетов
sudo tcpdump -i eth0 -A port 80
```
#### 13. Инструменты аппаратной диагностики
lscpu — информация о CPU
```bash
lscpu
lscpu | grep -E "Model name|CPU\(s\)|Thread|Core|Socket"
lscpu -e                  # расширенная таблица
lsblk, fdisk — диски
bash
lsblk -f
lsblk -o NAME,SIZE,TYPE,MOUNTPOINT,UUID
sudo fdisk -l
sudo fdisk -l /dev/sda
sudo parted -l
```
lspci, lsusb — устройства

```bash
lspci
lspci -v                  # подробно
lspci -k                  # с драйверами
lsusb
lsusb -t                  # дерево
```
dmidecode — SMBIOS
```bash
sudo dmidecode -t system
sudo dmidecode -t memory
sudo dmidecode -t processor
sudo dmidecode -t bios
```
sensors — датчики
```bash
sudo apt install lm-sensors
sudo sensors-detect       # однократная настройка
sensors
watch -n 2 sensors        # мониторинг
dmesg — сообщения ядра
bash
dmesg
dmesg | tail -50
dmesg | grep -i error
dmesg | grep -i -E "fail|error|warn"
dmesg -T                  # с человекочитаемым временем
dmesg -w                  # отслеживание в реальном времени
```
#### 14. Инструменты анализа журналов
journalctl
```bash
# Все сообщения
journalctl

# Ошибки текущей загрузки
journalctl -p err -b

# Служба
journalctl -u ssh
journalctl -u apache2 -f

# За период
journalctl --since "2026-09-20 10:00"
journalctl --since "1 hour ago"
journalctl --since today

# По процессу
journalctl _PID=1234

# По пользователю
journalctl _UID=1000

# Ядро
journalctl -k

# Последние N строк
journalctl -n 100

# В реальном времени
journalctl -f

# Размер журнала
journalctl --disk-usage

# Очистка
sudo journalctl --vacuum-time=7d
sudo journalctl --vacuum-size=500M
Анализ текстовых логов
bash
# Поиск ошибок
grep -i error /var/log/syslog
grep -i -E "fail|error|critical" /var/log/syslog | tail -50

# Подсчёт
grep -c "error" /var/log/syslog

# Отслеживание
tail -f /var/log/syslog

# За конкретную дату
grep "Sep 20" /var/log/syslog

# Аутентификация
tail -f /var/log/auth.log
grep "Failed password" /var/log/auth.log

# Аудит
sudo tail -f /var/log/audit/audit.log
sudo ausearch -m USER_LOGIN --start today
```
#### 15. Комплексные примеры диагностики
##### Пример 1: Высокая нагрузка на CPU
```bash
# 1. Определить общую картину
top -b -n 1 | head -20
uptime

# 2. Найти топ-процессы
ps -eo pid,ppid,user,cmd,%cpu --sort=-%cpu | head -10

# 3. Проверить очередь CPU
vmstat 1 5

# 4. Историческая нагрузка
sar -u

# 5. Проверить конкретный процесс
pidstat -p <PID> 1 5
```
##### Пример 2: Проблемы с диском
```bash
# 1. Проверить место
df -h
df -i

# 2. Проверить I/O
iostat -x 2 5

# 3. Найти процессы, активно пишущие на диск
iotop

# 4. SMART-статус
sudo smartctl -H /dev/sda
sudo smartctl -a /dev/sda | grep -i -E "reallocated|pending|uncorrectable"

# 5. Ошибки в dmesg
dmesg | grep -i -E "ata|sd[a-z]|I/O error"
```
##### Пример 3: Сетевые проблемы
```bash
# 1. Проверить интерфейсы
ip a
ip r
nmcli device status

# 2. Проверить шлюз
ping -c 4 $(ip r | awk '/default/ {print $3}')

# 3. Проверить DNS
cat /etc/resolv.conf
dig @8.8.8.8 ya.ru
nslookup ya.ru

# 4. Проверить внешний адрес
ping -c 4 8.8.8.8

# 5. Трассировка
traceroute -n 8.8.8.8

# 6. Проверить порты
ss -tlnp
nmap -p 80,443 ya.ru

# 7. Захватить трафик
sudo tcpdump -i eth0 -n host 8.8.8.8 -c 20
```
##### Пример 4: Проблемы с памятью
```bash
# 1. Общая картина
free -h

# 2. Топ по памяти
ps -eo pid,user,cmd,%mem --sort=-%mem | head -10

# 3. Проверить swap
vmstat 1 5
swapon --show

# 4. OOM-killer
dmesg | grep -i "out of memory"
journalctl -k | grep -i oom

# 5. Историческая статистика
sar -r
```
##### Пример 5: Проблемы с DNS
```bash
# 1. Проверить конфигурацию
cat /etc/resolv.conf
resolvectl status

# 2. Проверить доступность DNS-сервера
ping -c 2 8.8.8.8
nc -zv 8.8.8.8 53

# 3. Прямой запрос
dig @8.8.8.8 ya.ru A
dig @8.8.8.8 ya.ru +short

# 4. Обратный запрос
dig -x 8.8.8.8

# 5. Трассировка
dig +trace ya.ru

# 6. Проверить службу
systemctl status systemd-resolved
journalctl -u systemd-resolved -n 50
```
##### Пример 6: Диагностика проблем с SSH
```bash
# 1. Проверить службу
systemctl status ssh
ss -tlnp | grep :22

# 2. Проверить конфигурацию
sudo sshd -t
sudo sshd -T | grep -E "port|permit"

# 3. Логи
journalctl -u ssh -n 50
tail -f /var/log/auth.log

# 4. Проверить доступность
nc -zv 192.168.1.1 22

# 5. Попытка подключения с отладкой
ssh -vvv user@192.168.1.1

# 6. Захват трафика
sudo tcpdump -i eth0 port 22 -c 20
```
#### 16. Сводная шпаргалка по инструментам

Задача              | 	Быстрая команда
------------------- | -------------------------
Топ процессов по CPU	 | ps -eo pid,cmd,%cpu --sort=-%cpu | head
Топ процессов по RAM	 | ps -eo pid,cmd,%mem --sort=-%mem | head
Использование памяти	 | free -h
Использование диска	 | df -h
Inodes	 | df -i
I/O нагрузка	 | iostat -x 2 5
Очередь CPU	 | vmstat 1 5
История CPU	 | sar -u
Слушающие порты	 | ss -tlnp
Активные соединения	 | ss -tn state established
Маршруты	 | ip r
ARP	 | ip n
DNS-запрос	 | dig +short ya.ru
Обратный DNS	 | dig -x 8.8.8.8
Доступность | 	ping -c 4 8.8.8.8
Маршрут | traceroute -n 8.8.8.8
Сканирование портов	 | nmap -p 1-1000 192.168.1.1
Захват трафика	 | tcpdump -i eth0 -n port 80
Ошибки ядра	 | dmesg | grep -i error
Ошибки службы	 | journalctl -u <service> -p err
Ошибки за загрузку	 | journalctl -p err -b
